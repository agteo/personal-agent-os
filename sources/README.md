# Sources

Sources are original evidence supplied by the user.

Use:

- `sources/inbox/` for new items to process
- `sources/documents/` for longer documents
- `sources/notes/` for notes and meeting records

Do not silently rewrite original source files.

When useful, create a summary using `templates/source-summary.md` and link it from memory.

