# Runtime Adapters

Adapters explain how different AI runtimes use this workspace.

The core workspace is provider-independent:

- `AGENTS.md`
- `memory/`
- `sources/`
- `skills/`
- `workflows/`
- `system/`
- `logs/`

Provider-specific behavior belongs under `adapters/`.

Deleting one adapter should not affect user memory.

